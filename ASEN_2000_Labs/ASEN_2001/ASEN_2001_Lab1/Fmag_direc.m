%This function will return the magnitude F, and the directions of the forces
%dx, dy, and dz 
function V = Fmag_direc(cell_input)

V = str2num(cell_input);
%V is a 1x4 vector that contains the mag F, dx, dy, dz of a force

end