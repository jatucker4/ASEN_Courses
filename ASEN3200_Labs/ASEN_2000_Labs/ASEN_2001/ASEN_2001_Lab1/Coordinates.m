%This function will return the x, y, and z coordinates of an external
%force/moment as a numerical vector
function A = Coordinates(cell_input)
A = str2num(cell_input);
%A is a 1 x 3 vector containing x, y, z coordinates
end